model_path: /opt/data/private/models/Llama-2-7b-hf # Model name on hugginface or Model path
server: transformers # select one  from [rwkv,mamba,transformers,vllm,]
acceleration: "" # select one from [duo_attn,Snapkv,streaming_llm,ms-poe,selfextend,lm-inifinite(llama,gpt-j,mpt)，activation_beacon,tova]
port: "5002" # the port
device: "0" # The Gpu you want to use
torch_dtype: torch.bfloat16

#selfextend 
#parameter
selfextend_window_size: 1024 # for selfextend
selfextend_group_size: 32 # for selfextend
# transformers==4.38.2, flash_attn==2.5.6 

#Activation Beacon
#pip install -U FlagEmbedding

#tova
#pip install transformers==4.36.2 sentencepiece
tova_multi_state_size : 4096

# lm-infinite
#conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
# conda install -c conda-forge sentencepiece einops cudatoolkit-dev tqdm ipython datasets evaluate rouge-score protobuf accelerate langchain openai
# pip install transformers deepspeed

# H2o
# pip install crfm-helm
# pip install git+https://github.com/huggingface/transformers
# pip install lm-eval
